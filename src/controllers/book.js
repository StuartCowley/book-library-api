const express = require('express');
const { Book } = require('../models');

// const createBookControlloer = async (req, res) => {
//    const newBook = await Book.create(req.body);
//    res.status(201).json(newBook);
// };